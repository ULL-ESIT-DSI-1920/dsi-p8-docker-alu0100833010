export default class Avenger {
    constructor(data) {
        this.character = data.character;                          // Nombre del vengador
        this.name = data.name;                                    // Nombre real del vengador
        this.level = data.level;                                  // Nivel del vengador
        this.genre = data.genre;                                  // Género del vengador
        this.specie = data.specie;                                // Especie del vengador
        this.color = data.color;                                  // Color del vengador
    }
}